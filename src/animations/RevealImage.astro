<img src="pinkbackground.jpg" alt="" />

<style>
  img {
    width: 500px;
    height: 400px;
    opacity: 0;
    transform: scale(0.85);
    will-change: opacity, transform;
    animation: fade-in linear forwards;
    animation-timeline: view();
    animation-range: entry 10% cover 40%;
    animation-fill-mode: forwards;
  }

  @keyframes fade-in {
    to {
      opacity: 1;
      transform: scale(1);
    }
  }
</style>
