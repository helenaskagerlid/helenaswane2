<div class="gradient-blob"></div>

<style>
  :root {
    --gradient: linear-gradient(
      135deg,
      #ff758c 0%,
      #ff7eb3 25%,
      #752c18 50%,
      #fcb69f 75%,
      #2f962b 100%
    );
  }

  .gradient-blob {
    position: absolute;
    top: 33%;
    left: 15%;
    width: 55%;
    height: 33%;
    background: var(--gradient);
    background-size: 400%;
    animation:
      bg-animation 10s infinite alternate,
      blob-morph 10s infinite alternate;
    border-radius: 45% 55% 60% 40% / 55% 45% 65% 35%;
    filter: blur(30px) brightness(1.2);
    opacity: 0.35;
    z-index: 0;
  }

  @keyframes bg-animation {
    0% {
      background-position: left;
    }
    100% {
      background-position: right;
    }
  }

  @keyframes blob-morph {
    0% {
      background-position: left;
      border-radius: 45% 55% 60% 40% / 55% 45% 65% 35%;
      transform: scale(1);
    }
    50% {
      border-radius: 55% 45% 40% 60% / 50% 60% 40% 50%;
      transform: scale(1.05);
    }
    100% {
      border-radius: 40% 60% 70% 30% / 45% 55% 50% 60%;
      transform: scale(1.03);
      background-position: right;
    }
  }
</style>
